{
  "providers": ["node"],
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs_20", "python3", "gcc", "gnumake"]
    },
    "install": {
      "cmds": ["npm ci || npm install"]
    },
    "build": {
      "cmds": ["npm run build"]
    }
  },
  "start": {
    "cmd": "npm start"
  }
}
